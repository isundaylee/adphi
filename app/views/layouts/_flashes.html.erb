<% flash.each do |name, msg| %>
  <% if msg.is_a?(String) %>
    <div data-alert class="alert-box <%= name.to_s %>">
      <%= content_tag :div, msg.split("\n").join("<br/>").html_safe %>
      <a href="#" class="close">&times;</a>
    </div>
  <% end %>
<% end %>